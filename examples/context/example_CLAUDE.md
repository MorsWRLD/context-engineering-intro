# CLAUDE Rules (Example)

- Always write TypeScript with ESLint rules applied.
- Backend must include Jest tests.
- Database queries should use Prisma ORM.
- Flutter widgets should be stateful if they manage timers or async data.
- Always provide error handling in Express controllers.
